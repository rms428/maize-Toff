1. deep first allocation for tree
critical
2. no downward HR... does that make sense in terms of gravity / leakage already doing that?
3. stress computered before tree uptake (so stress[t+1] maybe looks more like stress by tree than stress[t])

--
1. fix rooting depth